import { RideTracker } from '../components/RideTracker'

export function RideTrackerPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-green-50 dark:from-gray-900 dark:to-gray-800 py-8 px-4">
      <RideTracker />
    </div>
  )
}
